cmake_minimum_required(VERSION 3.5)
project(ur5e_gripper_msgs)
find_package(ament_cmake REQUIRED)
find_package(rosidl_default_generators REQUIRED)
rosidl_generate_interfaces(${PROJECT_NAME} "srv/PlanExecute.srv")
ament_package()
